# Simply return 200, rate limited

```shell
Usage of /tmp/go-build811007725/command-line-arguments/_obj/exe/main:
  -burst-limit int
        quantity of requests to allow in bursts (default 10)
  -listen-url string
        address to listen and serve upon (default "http://localhost:8080")
  -rate-limit int
        milliseconds at which to limit the rate of requests (default 1000)
```
